import unittest

# Odkomentuj, jeśli te klasy znajdują się w shared.py
# from src.shared import CollisionInfo, CollisionSide

class TestCollisionData(unittest.TestCase):
    def test_collision_side_enum(self):
        """Sprawdza,czy kolizje mają zachowane wartości."""
        # self.assertTrue(hasattr(CollisionSide, 'TOP'))
        # self.assertTrue(hasattr(CollisionSide, 'BOTTOM'))
        pass

    def test_collision_info_initialization(self):
        """Sprawdza poprawność kolizji."""
        # info = CollisionInfo(collision=True)
        # self.assertTrue(info.collision)
        pass